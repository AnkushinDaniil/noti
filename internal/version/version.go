// Package version holds the single source of truth for the noti binary version.
package version

// Version is the current noti release version, reported by `noti version`
// and the broker /health endpoint.
const Version = "2.0.0"
